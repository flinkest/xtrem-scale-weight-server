# Server configuration
PORT=3000

# Scale configuration
SCALE_IP=192.168.4.1

# Note: Ports are correctly configured in server.js:
# - Send to scale: 4444 (UDP remote port XTREM side)  
# - Receive from scale: 5555 (UDP local port PC side)
# - Server uses streaming mode (no polling needed)